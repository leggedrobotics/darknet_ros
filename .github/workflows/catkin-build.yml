name: Build

on: [push]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-latest
    container: ros:melodic

    steps:
    - uses: actions/checkout@v2

    - name: workspace
      shell: bash
      run: |
        mkdir -p ./ws/src
        ln -s ${{github.workspace}} ./ws/src/darknet_ros

    - name: rosdep install
      working-directory: ws
      shell: bash
      run: |
        source /opt/ros/$ROS_DISTRO/setup.bash
        rosdep update
        rosdep install --from-paths src --ignore-src -r -y

    - name: catkin build
      working-directory: ws
      shell: bash
      run: |
        source /opt/ros/$ROS_DISTRO/setup.bash
        catkin build -DCMAKE_BUILD_TYPE=$BUILD_TYPE
